{"type":"object","properties":{"type":{"description":"### ⚠️ Requires [Sync](https://modrinth.com/mod/sync) to be installed! ⚠️\n\n## Prevent Sprinting Particles\n\n---\n\n\n[Power Types](https://origins.readthedocs.io/en/latest/types/power_types/)\n\nPrevents the particle effects that normally appear when an entity is sprinting.\n\nType ID: `sync:prevent_sprinting_particles`\n\n---\n\n### Examples\n\n```json\n{\n    \"type\": \"sync:prevent_sprinting_particles\"\n}\n```\nAlways prevents sprinting particles.\n\n```json\n{\n    \"type\": \"sync:prevent_sprinting_particles\",\n    \"condition\": {\n        \"type\": \"apoli:sneaking\"\n    }\n}\n```\nPrevents sprinting particles while sneaking (doing this is useless but you get the point).","markdownDescription":"### ⚠️ Requires [Sync](https://modrinth.com/mod/sync) to be installed! ⚠️\n\n## Prevent Sprinting Particles\n\n---\n\n\n[Power Types](https://origins.readthedocs.io/en/latest/types/power_types/)\n\nPrevents the particle effects that normally appear when an entity is sprinting.\n\nType ID: `sync:prevent_sprinting_particles`\n\n---\n\n### Examples\n\n```json\n{\n    \"type\": \"sync:prevent_sprinting_particles\"\n}\n```\nAlways prevents sprinting particles.\n\n```json\n{\n    \"type\": \"sync:prevent_sprinting_particles\",\n    \"condition\": {\n        \"type\": \"apoli:sneaking\"\n    }\n}\n```\nPrevents sprinting particles while sneaking (doing this is useless but you get the point)."}},"x-intellij-html-description":"<hr>\n<h2>title: Prevent Sprinting Particles (Power Type)\ndate: 2024-01-07</h2>\n<h1>Prevent Sprinting Particles</h1>\n<p><a href=\"https://origins.readthedocs.io/en/latest/types/power_types/\">Power Types</a></p>\n<p>Prevents the particle effects that normally appear when an entity is sprinting.</p>\n<p>Type ID: <code>sync:prevent_sprinting_particles</code></p>\n<h3>Fields</h3>\n<p>This power type has no additional fields beyond standard power fields (like <code>condition</code>).</p>\n<h3>Notes</h3>\n<ul>\n<li>Only prevents the visual particles</li>\n<li>Does not affect sprinting speed or mechanics</li>\n<li>Works on any entity, not just players</li>\n<li>The power must be active to prevent particles</li>\n</ul>\n<h3>Examples</h3>\n<pre><code class=\"language-json\">{\n    &quot;type&quot;: &quot;sync:prevent_sprinting_particles&quot;\n}\n</code></pre>\n<p>Always prevents sprinting particles.</p>\n<pre><code class=\"language-json\">{\n    &quot;type&quot;: &quot;sync:prevent_sprinting_particles&quot;,\n    &quot;condition&quot;: {\n        &quot;type&quot;: &quot;apoli:sneaking&quot;\n    }\n}\n</code></pre>\n<p>Prevents sprinting particles while sneaking (doing this is useless but you get the point).</p>\n"}